<Window x:Class="Content_Management_System.View.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Content_Management_System.View"
        xmlns:UserControl="clr-namespace:Content_Management_System.UserControls"
        xmlns:fa5="http://schemas.fontawesome.com/icons/"
        mc:Ignorable="d"
        
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        
        Title="Login" Height="500" Width="400"
        Closed="Window_Closed">
    <Border x:Name="GridBorder" Style="{StaticResource BorderedGridStyle}">
    <Grid Style="{DynamicResource GridStyle}" KeyboardNavigation.TabNavigation="Cycle">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="3.5*"/>
        </Grid.RowDefinitions>

        <UserControl:WindowHeader MaximizedButton="False"/>

        <!--<Image x:Name="UserImageIMG" Grid.Row="1" Source="/Media/UserImages/Blank1.png" VerticalAlignment="Center" Width="300" Height="200" />-->
        <fa5:SvgAwesome Grid.Row="1" Icon="Regular_UserCircle" Foreground="{DynamicResource Primary2Color}" Width="196" Height="196"/>

        <StackPanel Grid.Row="2" Orientation="Vertical" Margin="10" KeyboardNavigation.TabNavigation="Continue" KeyboardNavigation.DirectionalNavigation="Continue">
            <Label x:Name="UserNameLB" Style="{DynamicResource LabelStyle}" Width="310" Content="Username" FontSize="18"/>
            <TextBox x:Name="UserNameTB" Style="{DynamicResource TextBoxStyle}" KeyDown="UserNameTB_KeyDown" Width="300" Height="30" FontSize="15" IsTabStop="True" TabIndex="0" AcceptsReturn="False"/>
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Vertical" Margin="10" KeyboardNavigation.DirectionalNavigation="Continue">
            <Label x:Name="PasswordLB" Style="{DynamicResource LabelStyle}" Width="310" Content="Password" FontSize="18"></Label>
            <TextBox x:Name="PasswordTB" Style="{DynamicResource TextBoxStyle}" KeyDown="PasswordTB_KeyDown" Width="300" Height="30" FontSize="15" IsTabStop="True" TabIndex="1" AcceptsReturn="False"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Bottom">
            <Button x:Name="LoginBtn" Click="LoginBtn_Click" Style="{DynamicResource ButtonStyle}" KeyDown="LoginBtn_KeyDown" Content="Login" Width="200" Height="40" FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Center"></Button>
            <TextBlock x:Name="LoginMessageTbl" Foreground="{DynamicResource TextErrorColorBrush}" Visibility="Hidden" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center" Margin="0 0 0 15">Failed Login</TextBlock>
        </StackPanel>

        </Grid>
    </Border>
</Window>
