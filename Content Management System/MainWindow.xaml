<Window x:Class="Content_Management_System.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Content_Management_System"
        xmlns:view="clr-namespace:Content_Management_System.View"
        xmlns:uc="clr-namespace:Content_Management_System.UserControls" 
        xmlns:fa5="http://schemas.fontawesome.com/icons/"
        mc:Ignorable="d"
        
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        
        Closing="Window_Closing"
        
        Title="MainWindow" Height="800" Width="1300">
    <Border x:Name="GridBorder" Style="{StaticResource BorderedGridStyle}">
        <Grid Style="{DynamicResource GridStyle}">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="6*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="0.2*"/>
            </Grid.RowDefinitions>

            <uc:WindowHeader Grid.Row="0" Grid.ColumnSpan="2" MinimizedButton="True" MaximizedButton="True" CloseButton="True"/>

            <StackPanel x:Name="HeaderStackPanel" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <fa5:SvgAwesome Icon="Solid_Film" Foreground="{DynamicResource Secondary0Color}" Width="48" Height="48" Margin="0 0 8 0"/>
                <TextBlock x:Name="HeaderTextTbl" Grid.Row="1" Style="{DynamicResource HeaderTextBlockStyle}" Text="Movie Reviews"/>
            </StackPanel>

            <ScrollViewer x:Name="MovieScrollViewer" Style="{DynamicResource ScrollViewerStyle}" Grid.Row="2" HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Disabled" HorizontalContentAlignment="Left" Margin="10 10 10 10" Padding="2">
                <ItemsControl x:Name="MovieReviewItemsControl" ItemsSource="{Binding Reviews}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel x:Name="MovieReviewStackPanel" Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <uc:ReviewItem ReviewEdited="ReviewItem_ReviewEdited" EditButtonEnabled="{Binding DataContext.IsAdminLogged, RelativeSource={RelativeSource AncestorType=Window}}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>

                </ItemsControl>
            </ScrollViewer>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                <Button x:Name="AddReviewBtn" Style="{DynamicResource TableButtonStyle}" Click="AddReviewBtn_Click" Content="Add" Width="100" Height="30" HorizontalAlignment="Right" FontSize="15" Margin="10 0 0 0"/>
                <Button x:Name="DeleteReviewBtn" Style="{DynamicResource TableButtonStyle}" Click="DeleteReviewBtn_Click" Content="Delete" Width="100" Height="30" HorizontalAlignment="Right" FontSize="15" Margin="10 0 0 0"/>
            </StackPanel>

            <CheckBox x:Name="CheckAllCb" Grid.Row="3" Checked="CheckAllCb_Checked" Unchecked="CheckAllCb_Checked" Style="{DynamicResource CheckBoxStyle}" Content="Select All" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" HorizontalAlignment="Right" Margin="0 0 20 0">
                <CheckBox.LayoutTransform>
                    <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
                </CheckBox.LayoutTransform>
            </CheckBox>

        </Grid>
    </Border>
</Window>